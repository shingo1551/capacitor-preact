import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proub.cap4',
  appName: 'preact-starter',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
